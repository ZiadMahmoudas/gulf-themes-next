import { requireAdmin } from "@/lib/admin";
export default async function MediaPage(){const {supabase}=await requireAdmin();const {data}=await supabase.storage.from("media").list("",{limit:100,sortBy:{column:"created_at",order:"desc"}});return <><div className="admin-section-head"><div><small>STORAGE / MEDIA</small><h1>الوسائط</h1><p>الصور المستخدمة في المقالات والقوالب محفوظة داخل Supabase Storage.</p></div></div><section className="admin-panel"><p className="admin-help">رفع الصور بيتم مباشرة من محرر المقال أو المنتج. هذه الصفحة تعرض مجلدات التخزين الرئيسية.</p><div className="media-folder-grid">{data?.map((x:any)=><div key={x.name}><span>▧</span><b>{x.name}</b><small>{x.metadata?.size?`${Math.round(x.metadata.size/1024)} KB`:"مجلد / ملف"}</small></div>)}</div></section></>}
