import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/admin";import { ProductForm } from "@/components/admin/ProductForm";export default async function EditPlugin({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await requireAdmin();const {data}=await supabase.from("plugins").select("*").eq("id",id).maybeSingle();if(!data)notFound();return <ProductForm type="plugins" product={data}/>}
