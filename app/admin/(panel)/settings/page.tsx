import { requireAdmin } from "@/lib/admin";import { saveSettings } from "@/app/admin/(panel)/actions";
export default async function SettingsPage({searchParams}:{searchParams:Promise<{saved?:string}>}){const {supabase}=await requireAdmin();const q=await searchParams;const {data}=await supabase.from("site_settings").select("*").eq("id",1).maybeSingle();return <><div className="admin-section-head"><div><small>SITE / SETTINGS</small><h1>الإعدادات</h1><p>بيانات البراند والتواصل الأساسية.</p></div></div>{q.saved&&<div className="admin-alert success">تم حفظ الإعدادات.</div>}<form action={saveSettings} className="admin-panel settings-form"><div className="two-fields"><label className="admin-field"><span>اسم البراند</span><input name="brand_name" defaultValue={data?.brand_name||"ArabDEV"}/></label><label className="admin-field"><span>الإيميل</span><input name="email" defaultValue={data?.email||"ziadbobo78@gmail.com"}/></label></div><label className="admin-field"><span>وصف قصير</span><textarea name="tagline" rows={3} defaultValue={data?.tagline||""}/></label><label className="admin-field"><span>الهاتف</span><input name="phone" defaultValue={data?.phone||"01100133486"}/></label><div className="two-fields"><label className="admin-field"><span>Facebook</span><input name="facebook" dir="ltr" defaultValue={data?.facebook||""}/></label><label className="admin-field"><span>Instagram</span><input name="instagram" dir="ltr" defaultValue={data?.instagram||""}/></label></div><label className="admin-field"><span>LinkedIn</span><input name="linkedin" dir="ltr" defaultValue={data?.linkedin||""}/></label><button className="admin-primary" type="submit">حفظ الإعدادات</button></form></>}
